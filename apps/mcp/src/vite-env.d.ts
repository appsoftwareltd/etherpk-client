// The client's library reads `import.meta.env` and imports a markdown resource with `?raw`;
// both are Vite features, and this bundle is built by Vite, so the same ambient types apply.
/// <reference types="vite/client" />
