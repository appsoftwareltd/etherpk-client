# EtherPK Client. The public page is /home (/terms and /privacy redirect to the deployment's own
# legal pages); the application itself is a private, signed-in surface. The Client serves no
# sitemap, so this file advertises none.
User-agent: *
Allow: /
Disallow: /graphs
Disallow: /g/
Disallow: /dev/
Disallow: /auth/
