<!-- Theme URL page: the workspace is the (workspace) layout; nothing here.
     GraphWorkspace reads params.themeId on real navigations (deep link / popstate),
     exactly as it reads params.concept for a Document URL and params.assetId for an
     Asset tab (ADR 0023, extended 2026-09-20). -->
