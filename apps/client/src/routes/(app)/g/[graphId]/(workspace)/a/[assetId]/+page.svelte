<!-- Asset URL page: the workspace is the (workspace) layout; nothing here.
     GraphWorkspace reads params.assetId on real navigations (deep link / popstate),
     exactly as it reads params.concept for a Document URL. -->
